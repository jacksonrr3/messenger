<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>{{title}}</title>
    <link href="../../styles/base.scss" rel="stylesheet" />
    <link href="{{stylesheet}}" rel="stylesheet" />
  </head>

  <body>
    {{> header}}
    <main>
    {{{body}}}
    </main>
    {{> footer}}

    {{#each scripts}}
    <script defer src="{{this}}"></script>
    {{/each}}
  </body>
</html>